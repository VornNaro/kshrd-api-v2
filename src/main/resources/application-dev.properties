## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=20MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory

#file.upload.server.path=E://Education-IT/Korea HRD/FinalProject/kshrd/image
file.upload.server.path=/home/kshrd/image/

file.upload.client.path=/api/v1/image/**

file.base.url=https://api.kshrd-ite.com/api/v1/image/

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

jwt.secret=kshrd

#spring.data.rest.basePath=/api/v1/

logging.file.path=.
logging.file.name=user-requests.log
logging.level.org.springframework=INFO

allowed.origins=*
# allowed.origins=http://localhost:3000



management.endpoints.web.base-path=/actuator
management.endpoints.web.exposure.include=health, prometheus, metrics
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true

